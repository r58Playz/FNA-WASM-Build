diff --git a/src/joystick/emscripten/SDL_sysjoystick.c b/src/joystick/emscripten/SDL_sysjoystick.c
index cddd975a7..3d8b11cfb 100644
--- a/src/joystick/emscripten/SDL_sysjoystick.c
+++ b/src/joystick/emscripten/SDL_sysjoystick.c
@@ -324,7 +324,7 @@ static bool EMSCRIPTEN_JoystickOpen(SDL_Joystick *joystick, int device_index)
     joystick->nbuttons = item->nbuttons;
     joystick->naxes = item->naxes;
 
-    rumble_available = EM_ASM_INT({
+    rumble_available = MAIN_THREAD_EM_ASM_INT({
         let gamepads = navigator['getGamepads']();
         if (!gamepads) {
             return 0;
@@ -410,7 +410,7 @@ static bool EMSCRIPTEN_JoystickRumble(SDL_Joystick *joystick, Uint16 low_frequen
     SDL_joylist_item *item = (SDL_joylist_item *)joystick->hwdata;
 
     // clang-format off
-    bool result = EM_ASM_INT({
+    bool result = MAIN_THREAD_EM_ASM_INT({
         let gamepads = navigator['getGamepads']();
         if (!gamepads) {
             return 0;
diff --git a/src/locale/emscripten/SDL_syslocale.c b/src/locale/emscripten/SDL_syslocale.c
index 6983628c6..560e6c346 100644
--- a/src/locale/emscripten/SDL_syslocale.c
+++ b/src/locale/emscripten/SDL_syslocale.c
@@ -27,7 +27,7 @@
 bool SDL_SYS_GetPreferredLocales(char *buf, size_t buflen)
 {
     /* *INDENT-OFF* */ // clang-format off
-    EM_ASM({
+    MAIN_THREAD_EM_ASM({
         var buf = $0;
         var buflen = $1;
         var list = undefined;
diff --git a/src/misc/emscripten/SDL_sysurl.c b/src/misc/emscripten/SDL_sysurl.c
index 881de0129..0e6fb791e 100644
--- a/src/misc/emscripten/SDL_sysurl.c
+++ b/src/misc/emscripten/SDL_sysurl.c
@@ -28,6 +28,6 @@ EM_JS_DEPS(sdlsysurl, "$UTF8ToString");
 
 bool SDL_SYS_OpenURL(const char *url)
 {
-    EM_ASM(window.open(UTF8ToString($0), "_blank"), url);
+    MAIN_THREAD_EM_ASM(window.open(UTF8ToString($0), "_blank"), url);
     return true;
 }
diff --git a/src/video/SDL_video.c b/src/video/SDL_video.c
index 77a81143f..36b277c4f 100644
--- a/src/video/SDL_video.c
+++ b/src/video/SDL_video.c
@@ -5803,7 +5803,7 @@ bool SDL_ShowSimpleMessageBox(SDL_MessageBoxFlags flags, const char *title, cons
     if (!message) {
         message = "";
     }
-    EM_ASM({
+    MAIN_THREAD_EM_ASM({
         alert(UTF8ToString($0) + "\n\n" + UTF8ToString($1));
     },
             title, message);
