diff --git a/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeConstructorBuilder.Mono.cs b/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeConstructorBuilder.Mono.cs
index 603be1d0b1e..46e9782aaca 100644
--- a/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeConstructorBuilder.Mono.cs
+++ b/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeConstructorBuilder.Mono.cs
@@ -156,7 +156,32 @@ internal override Type GetParameterType(int pos)
             Justification = "Linker doesn't analyze RuntimeResolve but it's an identity function")]
         internal MethodBase RuntimeResolve()
         {
-            return type.RuntimeResolve().GetConstructor(this);
+            Type resolvedType = type.RuntimeResolve();
+            if (!mhandle.IsNullHandle())
+            {
+                MethodBase? resolved = GetMethodFromHandle(mhandle, resolvedType.TypeHandle);
+                if (resolved != null && ReferenceEquals(resolved.DeclaringType, resolvedType))
+                    return resolved;
+            }
+
+            Type[] parameterTypes;
+            if (parameters == null || parameters.Length == 0)
+            {
+                parameterTypes = Type.EmptyTypes;
+            }
+            else
+            {
+                parameterTypes = new Type[parameters.Length];
+                for (int i = 0; i < parameters.Length; ++i)
+                    parameterTypes[i] = parameters[i].InternalResolve();
+            }
+
+            const BindingFlags Flags = BindingFlags.Public | BindingFlags.NonPublic | BindingFlags.Instance | BindingFlags.Static | BindingFlags.DeclaredOnly;
+            ConstructorInfo? ctor = resolvedType.GetConstructor(Flags, binder: null, call_conv, parameterTypes, modifiers: null);
+            if (ctor != null)
+                return ctor;
+
+            throw new MissingMethodException(resolvedType.FullName, ConstructorInfo.ConstructorName);
         }
 
         public override object Invoke(object? obj, BindingFlags invokeAttr, Binder? binder, object?[]? parameters, CultureInfo? culture)
diff --git a/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeFieldBuilder.Mono.cs b/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeFieldBuilder.Mono.cs
index 2bf9bf01df3..d36c744f6a8 100644
--- a/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeFieldBuilder.Mono.cs
+++ b/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeFieldBuilder.Mono.cs
@@ -251,11 +251,26 @@ internal void ResolveUserTypes()
                 marshal_info.marshaltyperef = RuntimeTypeBuilder.ResolveUserType(marshal_info.marshaltyperef);
         }
 
+        [UnconditionalSuppressMessage("ReflectionAnalysis", "IL2075:UnrecognizedReflectionPattern",
+            Justification = "Linker doesn't analyze RuntimeResolve but it's an identity function")]
         internal FieldInfo RuntimeResolve()
         {
-            // typeb.CreateType() populates this.handle
-            var type_handle = new RuntimeTypeHandle((typeb.CreateType() as RuntimeType)!);
-            return GetFieldFromHandle(handle, type_handle);
+            Type resolvedType = typeb.RuntimeResolve();
+            RuntimeType runtimeType = (RuntimeType)resolvedType;
+
+            if (!handle.IsNullHandle())
+            {
+                FieldInfo resolved = GetFieldFromHandle(handle, runtimeType.TypeHandle);
+                if (ReferenceEquals(resolved.DeclaringType, resolvedType))
+                    return resolved;
+            }
+
+            const BindingFlags Flags = BindingFlags.Public | BindingFlags.NonPublic | BindingFlags.Instance | BindingFlags.Static | BindingFlags.DeclaredOnly;
+            FieldInfo? field = resolvedType.GetField(name, Flags);
+            if (field != null)
+                return field;
+
+            throw new MissingFieldException(resolvedType.FullName, name);
         }
 
         public override Module Module
diff --git a/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeILGenerator.Mono.cs b/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeILGenerator.Mono.cs
index ae834947bc0..f6330918589 100644
--- a/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeILGenerator.Mono.cs
+++ b/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeILGenerator.Mono.cs
@@ -347,8 +347,8 @@ public override void BeginCatchBlock(Type? exceptionType)
         {
             if (!InExceptionBlock)
                 throw new NotSupportedException(SR.Argument_NotInExceptionBlock);
-            if (exceptionType != null && exceptionType.IsUserType)
-                throw new NotSupportedException(SR.PlatformNotSupported_UserDefinedSubclassesOfType);
+            if (exceptionType != null)
+                exceptionType = RuntimeTypeBuilder.ResolveUserType(exceptionType);
             if (ex_handlers![cur_block].LastClauseType() == ILExceptionBlock.FILTER_START)
             {
                 if (exceptionType != null)
@@ -439,8 +439,7 @@ public override void BeginScope()
         public override LocalBuilder DeclareLocal(Type localType, bool pinned)
         {
             ArgumentNullException.ThrowIfNull(localType);
-            if (localType.IsUserType)
-                throw new NotSupportedException(SR.PlatformNotSupported_UserDefinedSubclassesOfType);
+            localType = RuntimeTypeBuilder.ResolveUserType(localType)!;
             RuntimeLocalBuilder res = new RuntimeLocalBuilder(localType, this);
             res.is_pinned = pinned;
 
diff --git a/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeMethodBuilder.Mono.cs b/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeMethodBuilder.Mono.cs
index c891daa7459..4ebc5f3cce2 100644
--- a/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeMethodBuilder.Mono.cs
+++ b/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeMethodBuilder.Mono.cs
@@ -271,9 +271,45 @@ internal override Type GetParameterType(int pos)
             return parameters![pos];
         }
 
+        [UnconditionalSuppressMessage("ReflectionAnalysis", "IL2075:UnrecognizedReflectionPattern",
+            Justification = "Linker doesn't analyze RuntimeResolve but it's an identity function")]
         internal MethodBase RuntimeResolve()
         {
-            return type.RuntimeResolve().GetMethod(this);
+            Type resolvedType = type.RuntimeResolve();
+            if (!mhandle.IsNullHandle())
+            {
+                MethodBase? resolved = GetMethodFromHandle(mhandle, resolvedType.TypeHandle);
+                if (resolved != null && ReferenceEquals(resolved.DeclaringType, resolvedType))
+                    return resolved;
+            }
+
+            Type[] parameterTypes;
+            if (parameters == null || parameters.Length == 0)
+            {
+                parameterTypes = Type.EmptyTypes;
+            }
+            else
+            {
+                parameterTypes = new Type[parameters.Length];
+                for (int i = 0; i < parameters.Length; ++i)
+                    parameterTypes[i] = parameters[i].InternalResolve();
+            }
+
+            const BindingFlags Flags = BindingFlags.Public | BindingFlags.NonPublic | BindingFlags.Instance | BindingFlags.Static | BindingFlags.DeclaredOnly;
+
+            if (name == ConstructorInfo.ConstructorName || name == ConstructorInfo.TypeConstructorName)
+            {
+                ConstructorInfo? ctor = resolvedType.GetConstructor(Flags, binder: null, call_conv, parameterTypes, modifiers: null);
+                if (ctor != null)
+                    return ctor;
+                throw new MissingMethodException(resolvedType.FullName, name);
+            }
+
+            MethodInfo? method = resolvedType.GetMethod(name, Flags, binder: null, call_conv, parameterTypes, modifiers: null);
+            if (method != null)
+                return method;
+
+            throw new MissingMethodException(resolvedType.FullName, name);
         }
 
         internal Module GetModule()
diff --git a/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeModuleBuilder.Mono.cs b/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeModuleBuilder.Mono.cs
index 91ba1108493..d9c134f9a63 100644
--- a/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeModuleBuilder.Mono.cs
+++ b/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeModuleBuilder.Mono.cs
@@ -851,7 +851,7 @@ public override int GetHashCode()
 
         public override bool IsDefined(Type attributeType, bool inherit)
         {
-            return base.IsDefined(attributeType, inherit);
+            return CustomAttribute.IsDefined(this, attributeType, inherit);
         }
 
         public override object[] GetCustomAttributes(bool inherit) => CustomAttribute.GetCustomAttributes(this, inherit);
diff --git a/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeTypeBuilder.Mono.cs b/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeTypeBuilder.Mono.cs
index 230c52dd304..87cb697e491 100644
--- a/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeTypeBuilder.Mono.cs
+++ b/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/RuntimeTypeBuilder.Mono.cs
@@ -41,6 +41,7 @@
 using System.Globalization;
 using System.Runtime.CompilerServices;
 using System.Runtime.InteropServices;
+using System.Runtime.Loader;
 
 namespace System.Reflection.Emit
 {
@@ -674,6 +675,22 @@ private bool has_ctor_method()
             return false;
         }
 
+        private static bool TryTypeResolve(RuntimeTypeBuilder typeBuilder)
+        {
+            Debug.Assert(!typeBuilder.is_created);
+
+            string? fullName = typeBuilder.FullName;
+            if (fullName == null)
+                return false;
+
+            RuntimeAssembly? requestingAssembly = typeBuilder.Assembly as RuntimeAssembly;
+            if (requestingAssembly == null && typeBuilder.Assembly is AssemblyBuilder assemblyBuilder)
+                requestingAssembly = Unsafe.As<RuntimeAssembly>(assemblyBuilder);
+
+            _ = AssemblyLoadContext.OnTypeResolve(requestingAssembly, fullName);
+            return typeBuilder.is_created;
+        }
+
         // We require emitted types to have all members on their bases to be accessible.
         // This is basically an identity function for `this`.
         [UnconditionalSuppressMessage("ReflectionAnalysis", "IL2083:UnrecognizedReflectionPattern",
@@ -702,7 +719,7 @@ protected override TypeInfo CreateTypeInfoCore()
                     if (!fb.IsStatic && (ft is RuntimeTypeBuilder builder) && ft.IsValueType && (ft != this) && is_nested_in(ft))
                     {
                         RuntimeTypeBuilder tb = builder;
-                        if (!tb.is_created)
+                        if (!tb.is_created && !TryTypeResolve(tb))
                         {
                             throw new NotImplementedException();
                         }
@@ -747,7 +764,7 @@ protected override TypeInfo CreateTypeInfoCore()
                         throw new BadImageFormatException();
                     if (!iface.IsInterface)
                         throw new TypeLoadException();
-                    if (iface is RuntimeTypeBuilder builder && !builder.is_created)
+                    if (iface is RuntimeTypeBuilder builder && !builder.is_created && !TryTypeResolve(builder))
                         throw new TypeLoadException();
                 }
             }
@@ -1506,13 +1523,78 @@ public override InterfaceMapping GetInterfaceMap([DynamicallyAccessedMembers(Dyn
 
         internal override Type InternalResolve()
         {
+            if (createTypeCalled && created == null && !is_hidden_global_type)
+            {
+                string? fullName = FullName;
+                if (fullName != null)
+                {
+                    RuntimeAssembly? requestingAssembly = Assembly as RuntimeAssembly;
+                    if (requestingAssembly == null && Assembly is AssemblyBuilder assemblyBuilder)
+                        requestingAssembly = Unsafe.As<RuntimeAssembly>(assemblyBuilder);
+
+                    _ = AssemblyLoadContext.OnTypeResolve(requestingAssembly, fullName);
+                    if (created != null)
+                        return created;
+                }
+
+                throw new NotSupportedException(SR.NotSupported_IncompleteTypes);
+            }
+
+            if (!is_created)
+            {
+                string? fullName = FullName;
+                if (fullName != null)
+                {
+                    RuntimeAssembly? requestingAssembly = Assembly as RuntimeAssembly;
+                    if (requestingAssembly == null && Assembly is AssemblyBuilder assemblyBuilder)
+                        requestingAssembly = Unsafe.As<RuntimeAssembly>(assemblyBuilder);
+
+                    _ = AssemblyLoadContext.OnTypeResolve(requestingAssembly, fullName);
+                    if (is_created)
+                        return created!;
+                }
+            }
             check_created();
             return created!;
         }
 
         internal override Type RuntimeResolve()
         {
+            if (createTypeCalled && created == null && !is_hidden_global_type)
+            {
+                string? fullName = FullName;
+                if (fullName != null)
+                {
+                    RuntimeAssembly? requestingAssembly = Assembly as RuntimeAssembly;
+                    if (requestingAssembly == null && Assembly is AssemblyBuilder assemblyBuilder)
+                        requestingAssembly = Unsafe.As<RuntimeAssembly>(assemblyBuilder);
+
+                    _ = AssemblyLoadContext.OnTypeResolve(requestingAssembly, fullName);
+                    if (created != null)
+                        return created;
+                }
+
+                throw new NotSupportedException(SR.NotSupported_IncompleteTypes);
+            }
+
+            if (!is_created)
+            {
+                string? fullName = FullName;
+                if (fullName != null)
+                {
+                    RuntimeAssembly? requestingAssembly = Assembly as RuntimeAssembly;
+                    if (requestingAssembly == null && Assembly is AssemblyBuilder assemblyBuilder)
+                        requestingAssembly = Unsafe.As<RuntimeAssembly>(assemblyBuilder);
+
+                    _ = AssemblyLoadContext.OnTypeResolve(requestingAssembly, fullName);
+                    if (is_created)
+                        return created!;
+                }
+            }
+
             check_created();
+            if (created == null && !is_hidden_global_type)
+                throw new NotSupportedException(SR.NotSupported_IncompleteTypes);
             return created!;
         }
 
diff --git a/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/SignatureHelper.cs b/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/SignatureHelper.cs
index 606dd993e52..c2e3fdb5aa4 100644
--- a/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/SignatureHelper.cs
+++ b/src/mono/System.Private.CoreLib/src/System/Reflection/Emit/SignatureHelper.cs
@@ -395,14 +395,14 @@ internal static SignatureHelper GetMethodSigHelper(Module? mod, CallingConventio
 
             returnType ??= typeof(void);
 
-            if (returnType.IsUserType)
-                throw new NotSupportedException(SR.PlatformNotSupported_UserDefinedSubclassesOfType);
+            returnType = RuntimeTypeBuilder.ResolveUserType(returnType)!;
+
+            Type[]? resolvedParameters = null;
             if (parameters != null)
             {
+                resolvedParameters = new Type[parameters.Length];
                 for (int i = 0; i < parameters.Length; ++i)
-                    if (parameters[i].IsUserType)
-                        throw new NotSupportedException(SR.PlatformNotSupported_UserDefinedSubclassesOfType);
-
+                    resolvedParameters[i] = RuntimeTypeBuilder.ResolveUserType(parameters[i])!;
             }
 
             SignatureHelper helper = new SignatureHelper((ModuleBuilder?)mod, SignatureHelperType.HELPER_METHOD);
@@ -410,11 +410,9 @@ internal static SignatureHelper GetMethodSigHelper(Module? mod, CallingConventio
             helper.callConv = callingConvention;
             helper.unmanagedCallConv = unmanagedCallingConvention;
 
-            if (parameters != null)
+            if (resolvedParameters != null)
             {
-                helper.arguments = new Type[parameters.Length];
-                for (int i = 0; i < parameters.Length; ++i)
-                    helper.arguments[i] = parameters[i];
+                helper.arguments = resolvedParameters;
             }
 
             return helper;
diff --git a/src/mono/mono/metadata/class-init.c b/src/mono/mono/metadata/class-init.c
index 1ab7410538c..b225002b215 100644
--- a/src/mono/mono/metadata/class-init.c
+++ b/src/mono/mono/metadata/class-init.c
@@ -974,6 +974,9 @@ mono_class_create_generic_inst (MonoGenericClass *gclass)
 		if (!gklass->wastypebuilder)
 			klass->inited = 1;
 
+		if (!klass->instance_size)
+			klass->instance_size = gklass->instance_size ? gklass->instance_size : MONO_ABI_SIZEOF (MonoObject);
+
 		if (klass->enumtype) {
 			/*
 			 * For enums, gklass->fields might not been set, but instance_size etc. is
diff --git a/src/mono/mono/metadata/object.c b/src/mono/mono/metadata/object.c
index 94b86c74d72..ec0038598be 100644
--- a/src/mono/mono/metadata/object.c
+++ b/src/mono/mono/metadata/object.c
@@ -7990,6 +7990,7 @@ gint32
 mono_class_value_size (MonoClass *klass, guint32 *align)
 {
 	gint32 size;
+	gint32 min_object_size = MONO_ABI_SIZEOF (MonoObject);
 
 	/* fixme: check disable, because we still have external revereces to
 	 * mscorlib and Dummy Objects
@@ -8005,7 +8006,21 @@ mono_class_value_size (MonoClass *klass, guint32 *align)
 		return 0;
 	}
 
-	size = size - MONO_ABI_SIZEOF (MonoObject);
+	if (G_UNLIKELY (size < min_object_size)) {
+		g_warning ("mono_class_value_size: invalid instance size %d for class %s.%s (kind=%d inited=%d size_inited=%d wastypebuilder=%d)",
+			size,
+			m_class_get_name_space (klass),
+			m_class_get_name (klass),
+			klass->class_kind,
+			klass->inited,
+			klass->size_inited,
+			klass->wastypebuilder);
+		if (align)
+			*align = 1;
+		return 0;
+	}
+
+	size = size - min_object_size;
 
 	g_assert (size >= 0);
 	if (align)
diff --git a/src/mono/mono/metadata/sre.c b/src/mono/mono/metadata/sre.c
index 47b303d86ba..e3182711deb 100644
--- a/src/mono/mono/metadata/sre.c
+++ b/src/mono/mono/metadata/sre.c
@@ -1069,7 +1069,7 @@ mono_image_create_token (MonoDynamicImage *assembly, MonoObjectHandle obj,
 	int how_collide;
 	how_collide = MONO_DYN_IMAGE_TOK_SAME_OK;
 
-	if (strcmp (klass->name, "RuntimeType") == 0) {
+	if (strcmp (klass->name, "RuntimeType") == 0 || strcmp (klass->name, "RuntimeTypeBuilder") == 0) {
 		MonoType *type = mono_reflection_type_handle_mono_type (MONO_HANDLE_CAST (MonoReflectionType, obj), error);
 		goto_if_nok (error, leave);
 		MonoClass *mc = mono_class_from_mono_type_internal (type);
@@ -2556,6 +2556,7 @@ reflection_setup_internal_class_internal (MonoReflectionTypeBuilderHandle ref_tb
 	klass->image = &dynamic_image->image;
 
 	klass->inited = 1; /* we lie to the runtime */
+	klass->instance_size = MONO_ABI_SIZEOF (MonoObject);
 	klass->name = mono_string_to_utf8_image (klass->image, ref_name, error);
 	goto_if_nok (error, leave);
 	klass->name_hash = mono_metadata_str_hash (klass->name);
@@ -2639,6 +2640,8 @@ reflection_setup_internal_class_internal (MonoReflectionTypeBuilderHandle ref_tb
 		// If we failed to create the parent, fail the child
 		if (!parent_type)
 			goto leave;
+
+		klass->parent = mono_class_from_mono_type_internal (parent_type);
 	}
 
 	// Push the child type and parent type to process later
@@ -3569,6 +3572,13 @@ typebuilder_setup_fields (MonoClass *klass, MonoError *error)
 		instance_size = MONO_ABI_SIZEOF (MonoObject);
 	}
 
+	/*
+	 * Keep a sane object size even if field setup fails before layout runs.
+	 * TypeBuilder marks size_inited early to avoid recursive setup crashes,
+	 * so initialize instance_size up front to the parent/object baseline.
+	 */
+	klass->instance_size = instance_size;
+
 	int fcount = tb->num_fields;
 	mono_class_set_field_count (klass, fcount);
 
@@ -3792,6 +3802,18 @@ ves_icall_TypeBuilder_create_runtime_class (MonoReflectionTypeBuilderHandle ref_
 	MonoType *type = MONO_HANDLE_GETVAL (MONO_HANDLE_CAST (MonoReflectionType, ref_tb), type);
 	MonoClass *klass = mono_class_from_mono_type_internal (type);
 
+	MonoReflectionTypeHandle ref_parent;
+	ref_parent = MONO_HANDLE_CAST (MonoReflectionType, MONO_HANDLE_NEW_GET (MonoObject, ref_tb, parent));
+	if (!MONO_HANDLE_IS_NULL (ref_parent)) {
+		MonoType *parent_type = MONO_HANDLE_GETVAL (ref_parent, type);
+		if (!parent_type)
+			parent_type = mono_reflection_type_handle_mono_type (ref_parent, error);
+		goto_if_nok (error, parent_sync_failed);
+
+		if (parent_type)
+			klass->parent = mono_class_from_mono_type_internal (parent_type);
+	}
+
 	MonoArrayHandle cattrs = MONO_HANDLE_NEW_GET (MonoArray, ref_tb, cattrs);
 	mono_save_custom_attrs (klass->image, klass, MONO_HANDLE_RAW (cattrs)); /* FIXME use handles */
 
@@ -3913,6 +3935,9 @@ failure:
 	mono_loader_unlock ();
 failure_unlocked:
 	return MONO_HANDLE_CAST (MonoReflectionType, NULL_HANDLE);
+
+parent_sync_failed:
+	return MONO_HANDLE_CAST (MonoReflectionType, NULL_HANDLE);
 }
 
 typedef struct {
@@ -4206,22 +4231,6 @@ mono_reflection_resolve_object (MonoImage *image, MonoObject *obj, MonoClass **h
 		}
 		*handle_class = mono_defaults.fieldhandle_class;
 		g_assert (result);
-	} else if (is_sre_type_builder (oklass)) {
-		MonoReflectionTypeBuilderHandle tb = MONO_HANDLE_NEW (MonoReflectionTypeBuilder, (MonoReflectionTypeBuilder*)obj);
-		MonoType *type = mono_reflection_type_get_handle (&MONO_HANDLE_RAW (tb)->type, error);
-		goto_if_nok (error, return_null);
-		MonoClass *klass;
-
-		klass = m_type_data_get_klass (type);
-		if (klass->wastypebuilder) {
-			/* Already created */
-			result = klass;
-		} else {
-			// TODO: make this work on netcore when working on SRE.TypeBuilder
-			g_assert_not_reached();
-		}
-
-		*handle_class = mono_defaults.typehandle_class;
 	} else if (strcmp (oklass->name, "SignatureHelper") == 0) {
 		MonoReflectionSigHelper *helper = (MonoReflectionSigHelper*)obj;
 		MonoMethodSignature *sig;
@@ -4293,7 +4302,9 @@ mono_reflection_resolve_object (MonoImage *image, MonoObject *obj, MonoClass **h
 
 		result = method;
 		*handle_class = mono_defaults.methodhandle_class;
-	} else if (mono_is_sre_method_builder (oklass) ||
+	} else if (
+               is_sre_type_builder (oklass) ||
+			   mono_is_sre_method_builder (oklass) ||
 			   mono_is_sre_ctor_builder (oklass) ||
 			   mono_is_sre_field_builder (oklass) ||
 			   is_sre_gparam_builder (oklass) ||
